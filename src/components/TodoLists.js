import Card from '../UI/Card';
import TodoList from './TodoList';

const TodoLists = () => {
  const handleDate = (event) => {};
  return (
    <>
      <Card></Card>
    </>
  );
};

export default TodoLists;
